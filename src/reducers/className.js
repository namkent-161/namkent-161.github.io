var initialState = ['className', '.net', 'angular', 'java', 'test']

const className = (state = initialState ,action) => {
    switch(action.type) {
        default: return state;
    }
}

export default className;